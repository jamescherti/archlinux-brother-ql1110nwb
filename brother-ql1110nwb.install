post_upgrade() {
  echo "Restart CUPS to load the new files: systemctl restart cups"
  echo
  echo "You can now register your new printer using the web interface at:"
  echo "  http://localhost:631/"
}

post_install() {
  post_upgrade;
}
